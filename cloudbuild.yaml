steps:
  - name: "gcr.io/cloud-builders/docker"
    args:
      [
        "build", 
        "-f",
        "Dockerfile",
        "--build-arg",
        "-t",
        "gcr.io/stylishopdev/mobsf:$COMMIT_SHA",
        ".",
      ]
 
  - name: "gcr.io/cloud-builders/docker"
    args: ["push", "gcr.io/stylishopdev/mobsf:$COMMIT_SHA"]
   
      
   # Deploy container image to Cloud Run
  - name: "gcr.io/cloud-builders/gcloud"
    args:
      - "run"
      - "deploy"
      - "mobsf-scan"
      - "--image"
      - "gcr.io/stylishopdev/mobsf:$COMMIT_SHA"
      - "--region"
      - "us-central1"
      - "--platform"
      - "managed"
      - "--allow-unauthenticated"
      - "--memory"
      - "2Gi"
      - "--cpu"
      - "1.0"
timeout: 12000s
